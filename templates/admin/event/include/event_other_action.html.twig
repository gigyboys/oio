<span class="ddt dd_tdfault">
	<a class="dd_item" target="_blank" href="{{ path('event_viewbyid', {'id': event.id}) }}">
		<span class="icon"><img src="{{ asset('image/view_icon.png') }}" /></span><span class="label">Aperçu</span>
	</a>
	<a class="dd_item" href="{{ path('admin_event_tags', {'event_id': event.id}) }}">
		<span class="icon"><img src="{{ asset('image/list_icon.png') }}" /></span><span class="label">Tags</span>
	</a>
	<a class="dd_item" href="{{ path('admin_event_schools', {'event_id': event.id}) }}">
		<span class="icon"><img src="{{ asset('image/list_icon.png') }}" /></span><span class="label">Etablissements</span>
	</a>
	<a class="dd_item" href="{{ path('admin_event_comments', {'event_id': event.id}) }}">
		<span class="icon"><img src="{{ asset('image/list_icon.png') }}" /></span><span class="label">Commentaires</span>
	</a>
	<a class="dd_item" href="{{ path('admin_event_gallery', {'event_id': event.id}) }}">
		<span class="icon"><img src="{{ asset('image/list_icon.png') }}" /></span><span class="label">Galeries</span>
	</a>
</span>